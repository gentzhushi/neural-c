# Machine learning in C

This is a simple study-project for machine learning techniques.

Should you want to compile and run it, just run the following script:
```sh
git clone https://github.com/gentzhushi/machine-learning-in-c
cd machine-learning-in-c/linear-regression && cc main.c -o main.o && ./main.o 1 2
```
